<%= simple_form_for [ @professor, @student_story ] do |f| %>
  <%= f.input :title %>
  <%= f.input :text %>
  <%= f.input :date %>
  <%= f.submit "Create a new student story", class: "btn btn-primary" %>
<% end %>
