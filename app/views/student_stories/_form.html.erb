<%= form_with(model: student_story) do |form| %>
  <% if student_story.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(student_story.errors.count, "error") %> prohibited this student_story from being saved:</h2>

      <ul>
        <% student_story.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :text, style: "display: block" %>
    <%= form.text_field :text %>
  </div>

  <div>
    <%= form.label :date, style: "display: block" %>
    <%= form.date_field :date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
